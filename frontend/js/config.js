// Point this at your Vercel deployment's API. Vercel defaults a project's
// production URL to https://<project-name>.vercel.app -- update this if
// your project name/domain ends up different.
window.APP_CONFIG = {
  apiBase: 'https://chicago-pickleball-tournament.vercel.app/api',
};
